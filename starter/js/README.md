# JavaScript starter (Node.js 22 LTS)

## Setup

```
cd starter/js
npm install
```

## Run

```
npm start              # runs agent.js against a sample ticket
npm test               # runs the shared evaluator against tests/tickets.json
```

## Contract

Export `solveTicket(ticket)` from `agent.js`. See AGENTS.md §6.2.
